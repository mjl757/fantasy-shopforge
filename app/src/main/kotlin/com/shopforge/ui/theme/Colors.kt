package com.shopforge.ui.theme

import androidx.compose.ui.graphics.Color

// ---------------------------------------------------------------------------
// Fantasy-themed palette — warm golds, deep browns, parchment tones
// Internal visibility so that tests can verify the palette values.
// ---------------------------------------------------------------------------

// Warm gold / amber
internal val Gold = Color(0xFFB8860B)       // dark goldenrod
internal val GoldLight = Color(0xFFFFD700)  // bright gold
internal val GoldContainer = Color(0xFFFFF3C4) // pale parchment gold

// Deep brown
internal val BrownDark = Color(0xFF3E1F00)  // very dark brown
internal val BrownMid = Color(0xFF6B3A2A)   // warm mid brown
internal val BrownLight = Color(0xFF8D5524) // lighter brown

// Parchment / off-white
internal val Parchment = Color(0xFFF5E6C8)  // warm off-white
internal val ParchmentDark = Color(0xFFE8D5A3)

// Accent — deep red/burgundy for errors / warnings
internal val BurgundyDark = Color(0xFF7B1D1D)
internal val BurgundyLight = Color(0xFFF44336)
